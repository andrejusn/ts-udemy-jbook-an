{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "start": "lerna run start --parallel",
    "initDevelop": "npm install --prefix ./packages/local-client/ ./packages/local-client/ && npm install --prefix ./packages/local-api/ ./packages/local-api/ && npm install --prefix ./packages/cli/ ./packages/cli/ && npm install && npm start",
    "initUse": "node ./packages/cli/dist/index.js serve"
  },
  "dependencies": {
    "n": "^8.1.0"
  }
}
